<div class="row">
	<div class="col-md-6 col-md-offset-3">
<%= form_for([@applicant_job_preference.applicant.user,@applicant_job_preference.applicant,@applicant_job_preference],:class=>"form") do |f| %>
  <% if @applicant_job_preference.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@applicant_job_preference.errors.count, "error") %> prohibited this applicant_job_preference from being saved:</h2>

      <ul>
      <% @applicant_job_preference.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :prefer_job_time,{:class=>"label label-primary"} %><br />
    <%= f.select :prefer_job_time,["Full Time","Part Time","Freelance","Internship","Temporary"]%>
  </div>
  <div class="form-group">
    <%= f.label :prefer_start_available ,{:class=>"label label-primary"}%><br />
    <%= f.date_select :prefer_start_available%>
  </div>
  <div class="form-group">
    <%= f.label :prefer_expected_salary ,{:class=>"label label-primary"}%><br />
    <%= f.text_field :prefer_expected_salary%>
  </div>

  <div class="form-group">
    <%= f.label :location_id,{:class=>"label label-primary"} %><br />
    <%= f.select(:location_id, Location.all_for_select) %>
  </div>
  <%= f.hidden_field :applicant_id %>
  <div class="actions">
    <%= f.submit "Submit",:class=>"btn btn-primary"%>
  </div>
<% end %>
</div></div>
